# GraphRAG Docker 环境变量示例
# 复制为 .env 后按需修改：cp .env.example .env

# Neo4j（图数据库）
NEO4J_PASSWORD=change-me-in-production

# PostgreSQL（关系数据库）
POSTGRES_PASSWORD=postgres

# 可选：Milvus 数据目录（默认使用 compose 中 volumes）
# DOCKER_VOLUME_DIRECTORY=./volumes
